// Cloudflare Workers（靜態資產模式）部署設定
// Workers Builds 的 deploy 指令用 `npx wrangler versions upload` 即可，
// 沒有 Worker 程式碼，純上傳 public/ 靜態檔。
{
  "name": "bubbob",
  "compatibility_date": "2026-07-03",
  "assets": {
    "directory": "./public"
  }
}
